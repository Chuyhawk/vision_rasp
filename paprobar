import RPi.GPIO as GPIO
import time

PIN = 17

GPIO.setmode(GPIO.BCM)
GPIO.setup(PIN, GPIO.OUT, initial=GPIO.LOW)

print("ON")
GPIO.output(PIN, GPIO.HIGH)   # debe dar ~3.3V
time.sleep(3)

print("OFF")
GPIO.output(PIN, GPIO.LOW)    # debe dar 0V
time.sleep(3)

GPIO.cleanup()
